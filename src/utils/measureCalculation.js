export const hlidar = (cv, doneCalulating) => {};

export const toppur = (cv, doneCalulating) => {};

export const bak = (cv, doneCalulating) => {};

export const hillur = (cv, doneCalulating) => {};

export const hurdar = (cv, doneCalulating) => {};
